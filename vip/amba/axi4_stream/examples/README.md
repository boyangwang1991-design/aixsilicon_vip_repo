# examples/

最小集成示例 DUT（展示如何把 `axi4_stream` VIP 接上一个简化 DUT）。

- `examples/axi4_stream_example_top.sv` — 示例顶层
- `examples/smoke/` — smoke 示例

> 示例仅用于演示 VIP 用法，不进入正式发布包（`release.py` 只复制 src/docs/qualification）。
