# Interrupt VIP Metadata — 依据 schema/vip_metadata.schema.yaml
schema_version: "1.0"

vip:
  id: VIP-IRQ-001
  name: interrupt
  vlnv: aix:vip:interrupt:1.0.0
  lifecycle: prototype
  owner: dv-platform
  license: Apache-2.0

protocol:
  family: system
  name: Interrupt
  revision: "受控：通用中断服务语义，见 docs/spec_control.md（规划中）"
  modes: [active_master, passive]

capabilities:
  pulse_level: planned
  mask_priority: planned
  storm: planned
  lost_duplicate_detect: planned

dependencies:
  - aix:vip:common:^1.0

tools:
  vcs: unsupported
  xcelium: unsupported
  questa: unsupported
  verilator: unsupported

quality:
  gate: V0_PROTOTYPE
  requirement_coverage: 0
  negative_test_pass_rate: 0

provenance:
  derived_from: []
  sbom: metadata/sbom.spdx.json
