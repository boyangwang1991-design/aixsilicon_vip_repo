# Interrupt Fault VIP（P2）

中断故障注入 VIP，与 `system/interrupt/` 协同。

- 优先级：**P2**
- 状态：规划中

## 计划能力

- stuck-at 中断；
- lost / duplicate 中断；
- interrupt storm；
- late interrupt；
- 中断 Safety Mechanism（如 watchdog、冗余路径）检测验证。
