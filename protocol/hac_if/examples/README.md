# HAC-IF VIP Examples

> 骨架目录。接入示例待填充。

- `hac_p0_aes_tb.sv`：P0 小型控制核（CTRL + EVENT）接入示例；
- `hac_p1_fir_tb.sv`：P1 流式核（CTRL + STREAM + EVENT）接入示例；
- `hac_p2_conv2d_tb.sv`：P2 主动访存核（CTRL + MEM + EVENT）接入示例。

> 具体 HAC 算法内容归 `ip-repo`；此处只放 VIP 接入与装配示例。
